<h1>Sign In</h1>

<%= form_with model: @user, url: sessions_login_path, method: :post, data: { turbo: false } do |form| %>
  <%= form.label :email %>
  <%= form.email_field :email %>

  <%= form.label :password %>
  <%= form.password_field :password %>

  <%= form.submit "Sign In" %>
<% end %>

